{
    "name": "my-project-devcontainer",
    "image": "mcr.microsoft.com/devcontainers/base:ubuntu",  // Any generic, debian-based image.
    "features": {
        "ghcr.io/devcontainers/features/docker-from-docker:1": { // docker run -it -v /:/host/ ubuntu chroot /host/ bash
            "version": "latest",
            "moby": true
        }
    }
}
